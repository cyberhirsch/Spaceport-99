# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
seed.json

# Native build output and the generated wrapper projects. `android/` is a
# Capacitor template regenerated from capacitor.config.ts on every build, so it
# is an artifact rather than source.
release
android
ios

